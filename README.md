# doc-lint

Documentation consistency checker. Validates style rules in `docs/*.yaml` against a shared rule library.

## Setup

```bash
pip install -r requirements.txt
pip install -e .
python3 -m pytest test_docs.py
```
