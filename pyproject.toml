[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "doc-lint"
version = "0.1.0"
description = "Documentation consistency checker"
dependencies = ["pytest>=7.0", "pyyaml>=6.0", "doc-rules"]

[project.entry-points.pytest11]
doc_lint = "doc_lint"
